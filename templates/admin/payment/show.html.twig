{% extends 'base.html.twig' %}

{% block title %}Payment{% endblock %}

{% block body %}
    <h1>Payment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payment.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ payment.date ? payment.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ payment.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_payment_index') }}">back to list</a>

    <a href="{{ path('app_admin_payment_edit', {'id': payment.id}) }}">edit</a>

    {{ include('admin/payment/_delete_form.html.twig') }}
{% endblock %}
